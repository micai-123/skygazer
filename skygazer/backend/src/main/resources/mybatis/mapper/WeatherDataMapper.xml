<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skygazer.weather.mapper.WeatherDataMapper">

    <sql id="Base_Column_List">
        id, location, temperature, feels_like, humidity, wind_speed, wind_direction, wind_scale,
        weather_condition, weather_description, air_quality_index, air_quality_level, pm25, pm10,
        uv_index, visibility, pressure, precipitation, record_time, data_source, created_at
    </sql>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM weather_data
    </select>

    <select id="findRecentByLocation" resultType="WeatherData">
        SELECT <include refid="Base_Column_List"/>
        FROM weather_data
        WHERE location = #{location} AND record_time >= #{startTime}
        ORDER BY record_time DESC
    </select>

    <select id="findFirstByLocationOrderByRecordTimeDesc" resultType="WeatherData">
        SELECT <include refid="Base_Column_List"/>
        FROM weather_data
        WHERE location = #{location}
        ORDER BY record_time DESC
        LIMIT 1
    </select>

    <insert id="insert" parameterType="WeatherData" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO weather_data (location, temperature, feels_like, humidity, wind_speed, wind_direction,
            wind_scale, weather_condition, weather_description, air_quality_index, air_quality_level,
            pm25, pm10, uv_index, visibility, pressure, precipitation, record_time, data_source)
        VALUES (#{location}, #{temperature}, #{feelsLike}, #{humidity}, #{windSpeed}, #{windDirection},
            #{windScale}, #{weatherCondition}, #{weatherDescription}, #{airQualityIndex}, #{airQualityLevel},
            #{pm25}, #{pm10}, #{uvIndex}, #{visibility}, #{pressure}, #{precipitation}, #{recordTime}, #{dataSource})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO weather_data (location, temperature, feels_like, humidity, wind_speed, wind_direction,
            wind_scale, weather_condition, weather_description, air_quality_index, air_quality_level,
            pm25, pm10, uv_index, visibility, pressure, precipitation, record_time, data_source)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.location}, #{item.temperature}, #{item.feelsLike}, #{item.humidity}, #{item.windSpeed}, #{item.windDirection},
            #{item.windScale}, #{item.weatherCondition}, #{item.weatherDescription}, #{item.airQualityIndex}, #{item.airQualityLevel},
            #{item.pm25}, #{item.pm10}, #{item.uvIndex}, #{item.visibility}, #{item.pressure}, #{item.precipitation}, #{item.recordTime}, #{item.dataSource})
        </foreach>
    </insert>

    <delete id="deleteAll">
        DELETE FROM weather_data
    </delete>

</mapper>
